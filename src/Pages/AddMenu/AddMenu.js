import React from 'react';
import useTitle from './../../hooks/useTitle';

const AddMenu = () => {
    useTitle('Add Menu')
    return (
        <div>
            <h2>This is Add menu page</h2>
        </div>
    );
};

export default AddMenu;